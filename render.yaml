
        services:
          - type: web
            runtime: static
            name: EmployeeManagement_System_frontend

            rootDir: frontend

            buildCommand: npm install && npm run build

            staticPublishPath: dist

            routes:
              - type: rewrite
                source: /*
                destination: /index.html